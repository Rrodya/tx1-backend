export enum RolesEnum {
  ADMIN="ADMIN",
  USER="USER",
  PASSENGER="PASSENGER",
  DRIVER="DRIVER",
}

export enum PaginationEnum {
  PAGE_SIZE=10,
}

export enum DriveStatus {
  "CREATED"=0,
  "ENDED"=1
}